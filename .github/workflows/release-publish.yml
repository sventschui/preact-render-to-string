name: Release

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        ref: "${{ event.release.target_commitish }}"
    - name: Use Node.js 12
      uses: actions/setup-node@v1
      with:
        node-version: 12.x
    - run: npm install
    - run: npm run build
    - run: npm test
      env:
        CI: true
    - run: git tag -d "${{ event.release.tag_name }}"
    - run: npm version "${{ event.release.tag_name }}"
    - run: git push --force origin refs/tags/${{ event.release.tag_name }}:refs/tags/${{ event.release.tag_name }}
